const {Router} = require('express');
const productRoutes = Router();


// productRoutes.get('/products',ProductsController.showProducts);
// productRoutes.get('/products/:categoria_id');
// productRoutes.get('/products/:fornecedor_id');

// productRoutes.get('/products/:id',ProductsController.showThatProduct);
// productRoutes.put('/products/:id',ProductsController.editThatProduct);
// productRoutes.patch('/products/:id',ProductsController.editThisAtribute);
// productRoutes.delete('/products/:id', ProductsController.removeAllProduct);

module.exports = productRoutes;